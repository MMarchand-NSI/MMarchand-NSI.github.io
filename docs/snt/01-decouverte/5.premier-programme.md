# Ton premier programme Python

Tu as déjà écrit de petits programmes sur le parcours blanc. On passe maintenant à un vrai **langage de programmation**, celui qu'on utilisera toute l'année : **Python**, simple à lire, gratuit, et employé partout dans le monde réel.

!!! note "Cours ici, exercices sur Capytale"
    Cette page est le **cours** : lis-la et observe les exemples. Les **exercices** se font sur **Capytale** (via l'ENT) : tes programmes s'y exécutent dans le navigateur, rien à installer. Ton professeur te donnera le lien de l'activité.

!!! warning "Ton matériel, c'est tes identifiants"
    En classe, tu as besoin de **deux identifiants** :

    - celui de la **session Windows**, pour ouvrir l'ordinateur ;
    - celui de l'**ENT**, pour accéder à Capytale.

    Ces identifiants sont l'**essentiel de ton matériel** en SNT : sans eux, tu ne peux pas travailler. Connais-les et garde-les accessibles **à chaque séance**. Les oublier, c'est comme venir sans ses affaires : il faut alors chercher une solution, ce qui n'est **jamais immédiat** et **ralentit toute la classe**.

!!! info "Se connecter la première fois"
    1. Allume l'ordinateur et ouvre la **session Windows** avec tes identifiants.
    2. Ouvre le navigateur, puis va sur l'**ENT** de l'établissement.
    3. Connecte-toi à l'ENT avec tes **identifiants ENT**.
    4. Ouvre **Capytale** (dans l'ENT, via le **Médiacentre** ou les services numériques).
    5. Colle le **code ou le lien de l'activité** donné par ton professeur, et c'est parti.

## Afficher quelque chose

L'instruction `print` affiche un message à l'écran. Ce qui est entre guillemets est affiché tel quel.

!!! note "Pourquoi « print » ?"
    `print` veut dire **imprimer** en anglais. Aux débuts de l'informatique, il n'y avait pas d'écran : les résultats sortaient sur du **papier**, imprimés. Les écrans sont arrivés ensuite, mais le mot, lui, est resté.

```python
print("Bonjour")
print("Bienvenue en SNT")
```

Ce programme affiche deux lignes :

```text
Bonjour
Bienvenue en SNT
```

Attention à un point important : les **guillemets** changent tout. Compare :

```python
print("3 + 4")
print(3 + 4)
```

Ce programme affiche :

```text
3 + 4
7
```

La première ligne est **entre guillemets** : c'est du texte, recopié tel quel. La seconde est **sans guillemets** : Python la lit comme un calcul et affiche le résultat.

## Calculer

Sans guillemets, Python fait les calculs. Il connaît `+`, `-`, `*` (multiplier) et `/` (diviser).

```python
print(2026 - 2009)
print(6 * 7)
```

affiche :

```text
17
42
```

L'intérêt d'écrire un calcul plutôt que le résultat, c'est que Python ne se trompe jamais dans le calcul, et que si les nombres changent, le programme se met à jour tout seul.

## Retenir une valeur : la variable

Un ordinateur ne retient **rien** tout seul. La **seule** façon de garder une valeur pour la réutiliser plus tard, c'est de la **ranger en mémoire** dans une **variable** : une petite **boîte dans la mémoire** de l'ordinateur, avec une **étiquette** dessus, son **nom**. Il n'y a pas d'autre moyen de se souvenir de quelque chose.

```python
age = 16
print(age)
print(age + 1)
```

affiche :

```text
16
17
```

La ligne `age = 16` range la valeur 16 dans la boîte étiquetée `age`. Ensuite, partout où on écrit `age`, Python le remplace par sa valeur. On range une valeur dans la boîte, et on peut la rouvrir quand on veut.

Une variable peut aussi être **calculée** à partir d'une autre :

```python
annee = 2009
age = 2026 - annee
print(age)
```

affiche `17`. L'intérêt : si on change `annee`, l'âge se recalcule tout seul.

### Le nom, c'est toi qui choisis

Le nom d'une variable, c'est **le programmeur qui le choisit** : Python s'en moque. Ces deux programmes font exactement la même chose et affichent tous les deux `17` :

```python
annee = 2009
age = 2026 - annee
print(age)
```

```python
x = 2009
y = 2026 - x
print(y)
```

Alors autant choisir des noms qui **disent ce qu'il y a dans la boîte**. `annee` et `age` se relisent tout seuls ; `x` et `y`, non : dans six mois (ou même demain), tu ne sauras plus ce qu'ils contiennent. **Tu es libre du nom, mais choisis un nom parlant.**

### Attention : le `=` range, il n'additionne pas

On croit souvent que si on écrit deux valeurs de suite dans une variable, elles s'ajoutent. C'est faux : **chaque nouvelle valeur remplace l'ancienne**, qui est alors perdue.

```python
a = 4
a = 6
print(a)
```

affiche :

```text
6
```

La valeur 4 a été **écrasée** par 6. Dans la boîte `a`, il n'y a jamais qu'une seule valeur à la fois : la dernière rangée. Le signe `=` ne veut pas dire « égal » comme en maths ; il veut dire **« range la valeur de droite dans la variable de gauche »**.

Du coup, si on veut vraiment **ajouter** à une variable, il faut le dire explicitement :

```python
score = 10
score = score + 5
print(score)
```

affiche `15` : Python calcule d'abord `score + 5` (soit `10 + 5`), puis range le résultat à nouveau dans `score`.

## Lire, puis modifier

En programmation, on passe beaucoup de temps à **lire du code écrit par d'autres** et à le **modifier**, avant d'écrire le sien de zéro. C'est normal, et on s'y entraînera toute l'année.

Savoir lire, c'est aussi savoir repérer une erreur. Par exemple, ce programme ne fonctionne pas :

```python
print(Bonjour)
```

Il manque les guillemets : `Bonjour` sans guillemets n'est pas du texte pour Python, qui affiche une erreur. La version correcte est :

```python
print("Bonjour")
```

## Ce qu'on retient

- **Python** exécute vraiment les algorithmes qu'on écrit.
- `print(...)` affiche : entre **guillemets** c'est du texte recopié, **sans guillemets** un calcul est effectué.
- Une **variable** est une **boîte dans la mémoire**, étiquetée par son nom (`age = 16`), où l'on range une valeur pour la réutiliser.
- Le **nom** est libre (c'est toi qui choisis), mais on le choisit **parlant** : il dit ce qu'il y a dans la boîte.
- On progresse d'abord en **lisant et en modifiant** du code existant.
- Le cours est ici ; on s'entraîne sur **Capytale**.
