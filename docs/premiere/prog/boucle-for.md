# La boucle bornée : `for`

!!! note "Page en construction"
    Plan prévu (répéter un nombre **connu** de fois) :

    - Le besoin : répéter une action un nombre déterminé de fois.
    - `for i in range(n)` : ce que vaut `i` à chaque tour (tracer la machine, Sorva).
    - `range(a, b)` et `range(a, b, pas)`.
    - L'accumulation sur un intervalle d'entiers (somme, produit).
    - Prédire la sortie avant d'exécuter (PRIMM).
    - Exercices corrigés.

    Le parcours d'une **séquence** (`for x in liste`) est traité à part, dans [Itérer sur une séquence](bouclefor.md), car son mécanisme est différent.
