# Les conditionnelles

!!! note "Page en construction"
    Plan prévu :

    - Rappel : une condition est une **expression booléenne** (lien vers les booléens).
    - `if` seul : exécuter un bloc *seulement si* la condition est vraie.
    - `if / else` : deux chemins possibles.
    - `if / elif / else` : trois chemins ou plus.
    - Conditions imbriquées, et pourquoi on peut souvent les éviter.
    - **Lire et prédire avant d'écrire** (PRIMM) : tracer l'exécution d'un `if` donné, prédire la sortie, puis écrire.
    - Exercices corrigés.
